// To increment the COUNTER value by 1
export const increment = () => {
  return {
    type: "INCREMENT",
  };
};

// To decrement the COUNTER value by 1
export const decrement = () => {
  return {
    type: "DECREMENT",
  };
};
